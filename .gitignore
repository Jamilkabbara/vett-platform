logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.design-reference/

# Pass 38 DEPLOY-3 — version.json is regenerated by scripts/write-version.js
# at every build. Each commit's SHA is captured at build time, so storing
# the file in git would churn it on every commit.
public/version.json
